# Core
pdftext==0.6.3
numpy>=1.24
googletrans==4.0.0-rc1
rich>=13.0
markdown>=3.5

# googletrans pins these — listed explicitly so the venv is reproducible
httpx==0.13.3
httpcore==0.9.1
h11==0.9.0
h2==3.2.0
